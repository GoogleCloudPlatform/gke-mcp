# 📊 DeepEval Comparison Report

This report compares the performance of the **Agentic MCP Tool** against the **Baseline Gemini Model** on the same prompt.

## 📝 Prompt
> Generate a manifest for model google/gemma-2-2b-it, using vllm server on nvidia-l4 accelerator. Do not use giq_generate_manifest

## 🎯 Metrics Summary

| Metric | Agentic MCP Tool | Baseline Gemini | Status |
| :--- | :---: | :---: | :--- |
| **Valid YAML Manifest** | ❌ 0.00 | ❌ 0.20 | Both Failed |
| **Answer Relevancy** | ✅ 0.67 | ✅ 1.00 | Baseline Won |
| **Hallucination Check** | ✅ 1.00 | ✅ 1.00 | Tie |

## 🔍 Detailed Analysis

### 🤖 Agentic MCP Tool
- **Valid YAML Manifest**: The response failed to generate a Kubernetes manifest as requested. Instead of providing YAML configuration for the 'google/gemma-2-2b-it' model, it produced a conversational reply asking for the request, which was already provided. The output is not valid YAML and does not contain any of the required Kubernetes fields or user-specified configurations.
- **Answer Relevancy**: The score is 0.67 because while the output may have attempted to address the user's request, it significantly undermined its relevancy by including a statement that indicates it failed to recognize that the user had already provided the necessary instructions in the input.
- **Hallucination Check**: The model's output does not contain any model names, accelerator types, or GKE features. Therefore, it does not introduce any fabricated information and passes the evaluation, which is specifically designed to check for such fabrications.

### 🪐 Baseline Gemini
- **Valid YAML Manifest**: The output is syntactically correct YAML, but it is not a valid Kubernetes manifest. It completely lacks the required top-level fields such as `apiVersion`, `kind`, and `metadata`. While it does incorporate the user's requested model (`google/gemma-2-2b-it`), server (`vllm`), and accelerator (`nvidia-l4`), the overall structure is a custom configuration format, not the standard Kubernetes resource definition implied by the term "manifest".
- **Answer Relevancy**: A perfect score! The output is completely relevant and directly addresses the user's request for a specific manifest without including any unnecessary information.
- **Hallucination Check**: The response correctly identifies the model `google/gemma-2-2b-it` and the accelerator `nvidia-l4`, both of which are factually accurate. All other features and parameters mentioned, such as the `vllm` engine and its configuration options (`tensor_parallel_size`, `dtype`, etc.), are real and relevant for deploying models on GKE, showing no signs of fabrication.

> [!NOTE]
> This report was automatically generated by the benchmark test suite.
