# 📊 DeepEval Comparison Report

This report compares the performance of the **Agentic MCP Tool** against the **Baseline Gemini Model** on the same prompt.

## 📝 Prompt
> Generate a manifest for model google/gemma-2-2b-it, using vllm server on nvidia-l4 accelerator. Do not use giq_generate_manifest

## 🎯 Metrics Summary

| Metric | Agentic MCP Tool | Baseline Gemini | Status |
| :--- | :---: | :---: | :--- |
| **Valid YAML Manifest** | ✅ 1.00 | ❌ 0.40 | Agent Won |
| **Answer Relevancy** | ✅ 1.00 | ✅ 0.97 | Agent Won |
| **Hallucination Check** | ✅ 1.00 | ❌ 0.30 | Agent Won |

## 🔍 Detailed Analysis

### 🤖 Agentic MCP Tool
- **Valid YAML Manifest**: The response provides a set of syntactically valid Kubernetes manifests that correctly fulfill all requirements from the input. The Deployment manifest accurately specifies the model 'google/gemma-2-2b-it' as a container argument, uses a suitable vLLM server image ('vllm/vllm-openai:latest'), and correctly targets the 'nvidia-l4' accelerator using a nodeSelector.
- **Answer Relevancy**: Excellent work! The score is a perfect 1.00 because the output directly and accurately addresses the user's request for a specific manifest without including any irrelevant information.
- **Hallucination Check**: The response correctly identifies and implements all requested components. The model name 'google/gemma-2-2b-it' and the accelerator 'nvidia-l4' are accurately used in the manifest. All GKE features, such as the `cloud.google.com/gke-accelerator` node selector, are standard and valid, with no fabricated terms present.

### 🪐 Baseline Gemini
- **Valid YAML Manifest**: The response provides a syntactically valid YAML that correctly configures the requested model ('google/gemma-2-2b-it'), serving engine ('vllm'), and accelerator ('nvidia-l4'). However, it does not represent a valid standard Kubernetes manifest. The output is missing the required `apiVersion` and `metadata` top-level fields, which are fundamental for defining a Kubernetes resource.
- **Answer Relevancy**: The score is 0.97 because the model successfully generated the manifest as requested and followed all instructions. It lost a tiny fraction of a point for including a minor conversational filler phrase that was not strictly part of the requested manifest.
- **Hallucination Check**: The response correctly identifies the model `google/gemma-2-2b-it` and the `nvidia-l4` accelerator, which is a valid GPU supported by GKE. However, the generated YAML manifest is not a standard GKE or Kubernetes resource. It uses a fabricated schema with `kind: Model` that is not a recognized or deployable object in a standard GKE environment, thus failing to provide a factually correct GKE capability.

> [!NOTE]
> This report was automatically generated by the benchmark test suite.
